# 贪吃蛇（Snake）

一个运行在浏览器中的经典贪吃蛇小游戏，当前版本 **v1.0**。

## 使用技术

- 原生 HTML5
- 原生 CSS3
- 原生 JavaScript（ES5/ES6，无框架、无第三方库）
- HTML5 Canvas 2D 绘图

## 版本历史

| 版本 | 内容 |
| --- | --- |
| v0.1 | 搭建项目基础结构：`index.html`、`style.css`、`game.js`、`README.md`，Canvas 居中显示与基础背景样式 |
| v0.2 | 蛇的网格移动、WASD 与方向键控制、屏幕上下左右方向按钮、禁止 180° 回头与两步转向缓冲（当时为边界穿墙） |
| v0.3 | 食物与得分系统：吃到食物蛇身 +1 节，按“第 n 个食物得 n 分”累加；Canvas 上方的新增得分与状态栏，操作提示改为 HTML 覆盖层 |
| v1.0 | 死亡机制（撞墙与自碰）、GAME OVER 与结束卡片、暂停/继续、重新开始、长度显示与 UI 完善，首个可正式游玩的版本 |

## 当前版本功能

- 24 x 24 网格（每格 25px）上的蛇身绘制，蛇头与蛇身用颜色区分
- 固定节奏移动，每秒 8 格
- 键盘控制：WASD 与方向键等价，两个方向均可操作
- 屏幕方向按钮：Canvas 下方的上下左右十字按钮，点击或触摸即可转向
- 转向规则：禁止 180° 回头，同一移动间隔内最多缓存两步转向
- 食物：随机生成在空格上的橙红色圆形，吃到后立即在其它空格重新生成
- 得分与成长：吃到食物蛇身 +1 节，并按“第 n 个食物得 n 分”累加
- 死亡判定：撞到墙壁或咬到自己即结束游戏；追着尾巴跑（头进入本 tick 会移开的尾格）不算死亡
- 暂停与继续：暂停按钮或空格键暂停，可通过按钮、空格键或任意方向输入继续
- 重新开始：暂停卡片与结束卡片都提供“重新开始”，无需刷新页面即可回到初始状态
- HTML UI：游戏标题、得分、长度、状态、暂停按钮、操作提示与两张遮罩卡片

尚未实现：

- 历史最高分记录
- 速度随进度递增、音效等附加内容

## 计分规则

第 n 个食物得 n 分，累计总分为 1、3、6、10、15……
规则集中在 `calculateFoodScore()` 中，后续替换为更复杂的算法只需修改这个函数。

## 分层约定

Canvas 只负责游戏内容的绘制：背景、网格、食物与蛇。
分数、状态文案、操作提示与方向按钮等 UI 全部由 HTML 与 CSS 负责，
操作提示通过 `.canvas-stage` 内的绝对定位覆盖层显示在 Canvas 上方。

## 操作方式

页面打开后蛇静止不动，Canvas 内蛇的下方会显示渐显渐隐的操作提示，
按 WASD、方向键或下方方向按钮后开始移动，提示同时消失，状态栏变为“进行中”。
按空格键或点击“暂停”按钮可暂停游戏，暂停后点击“继续游戏”、按空格键、
按任意方向键或点击屏幕方向按钮都能恢复；恢复时蛇保持原来的方向。
撞到墙壁或咬到自己会进入“游戏结束”状态，此时蛇与分数定格，
在结束卡片上点击“重新开始”即可重新开局。
如果按键没有反应，先用鼠标点击一下画布，让页面获得键盘焦点。

## 状态说明

| 状态 | 界面文案 | 说明 |
| --- | --- | --- |
| `ready` | 准备开始 | 蛇静止等待，按方向键开始 |
| `running` | 进行中 | 蛇按固定节奏移动 |
| `paused` | 已暂停 | 移动冻结，显示暂停卡片 |
| `over` | 游戏结束 | 撞墙或自碰后定格，显示结束卡片 |

## 文件说明

| 文件 | 说明 |
| --- | --- |
| `index.html` | 页面结构，承载标题、得分/长度/状态栏、暂停按钮、Canvas 游戏区域、方向按钮与两张遮罩卡片 |
| `style.css` | 页面布局、提示动画、按钮与卡片样式等视觉样式 |
| `game.js` | 游戏状态机、主循环、输入处理、食物与得分逻辑、死亡与暂停判定、Canvas 绘制 |
| `README.md` | 项目说明文档 |

## 调试接口

浏览器控制台可通过 `window.__snakeDebug.getState()` 查看当前状态快照
（`state`、`direction`、`head`、`snakeLength`、`snake`、`food`、
`foodEaten`、`score`、`pendingDirections`），
该接口只读，仅供调试与自动化验证使用。
